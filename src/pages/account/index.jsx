import { Box, Container, Typography } from "@mui/material"

const Account = () => {
    return (
        <Box>
            <Typography
                variant="h1"
            >
                Account
            </Typography>
        </Box>
    )
};

export default Account;